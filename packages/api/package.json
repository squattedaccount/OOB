{
  "name": "oob-api",
  "version": "0.1.0",
  "private": true,
  "description": "Open Order Book API — public REST API for Seaport v1.6 orders",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "postgres": "^3.4.8",
    "typescript": "^5.4.0",
    "wrangler": "^4.58.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "ethers": "^6.16.0"
  }
}
